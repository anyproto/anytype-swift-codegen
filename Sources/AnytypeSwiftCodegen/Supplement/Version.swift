/// This file only keep its default state.
/// It only changes its content on build but later it reset it to default state.
/// So, it produces copies that contain current version.
/// This message will be only during builds.
public let ToolVersion = "undefined version: run 'make version' to update"
